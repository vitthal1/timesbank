// PostCSS config for Tailwind CSS v4 integration.
// Uses @tailwindcss/postcss plugin; no autoprefixer needed (v4 handles prefixes internally).

export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};